# apiVersion: cert-manager.io/v1
# kind: Issuer
# metadata:
#   name: cluster-issuer
# spec:
#   acme:
#     solvers:
#     - dns01:
#         cloudDNS:
#           project: {{ .Values.global.project_id }}
#           serviceAccountSecretRef:
#             name: clouddns-dns01-solver-svc-acct
#             key: key.json

# ---

# apiVersion: cert-manager.io/v1
# kind: Certificate
# metadata:
#   name: quelle-indignite-com
#   namespace: default
# spec:
#   secretName: quelle-indignite-com-tls
#   issuerRef:
#     name: cluster-issuer
#   dnsNames:
#   - {{ .Values.domain}}
#   - "www.{{ .Values.domain }}"
